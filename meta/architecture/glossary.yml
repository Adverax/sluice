# generated_by: forge:architect-domain-extraction
# generated_at: 2026-06-24T00:00:00Z
# tier: large

terms:
  - id: TERM-001
    term: ApiKey
    definition: >
      A string identifier passed in the Authorization header. In v1 it is used
      exclusively as a key for rate limiting and usage metrics; validation against
      an identity store is not performed.
    context: unknown
    aliases: [API key, api_key, authorization_key]
    source: [US-001, US-005, US-015]

  - id: TERM-002
    term: InferenceRequest
    definition: >
      A text-generation (chat completion) request sent by the API Client to
      POST /v1/chat/completions. Contains the model, messages, and a streaming flag.
    context: unknown
    aliases: [chat completion request, inference request]
    source: [US-001, US-002, US-003]

  - id: TERM-003
    term: Provider
    definition: >
      A Go interface that abstracts an LLM backend (provider). In v1 it is represented
      by a managed mock with configurable latency and error rate. It serves as an
      extension point for adding real providers.
    context: unknown
    aliases: [LLM Provider, upstream]
    source: [US-003, US-007, US-008]

  - id: TERM-004
    term: RateLimit
    definition: >
      A per-API-key request-rate constraint implemented via a token bucket.
      Local state is stored in `x/time/rate`; distributed state is stored in Redis.
      When the limit is exceeded, a 429 is returned with a Retry-After header.
    context: unknown
    aliases: [rate limiting, request limit, ratelimit]
    source: [US-005]

  - id: TERM-005
    term: CircuitBreaker
    definition: >
      A per-provider component (sony/gobreaker) that tracks a series of errors.
      When the threshold is reached it transitions to the open state, immediately
      rejecting all requests to the provider without calling upstream until the
      recovery timeout expires.
    context: unknown
    aliases: [circuit breaker, breaker]
    source: [US-008]

  - id: TERM-006
    term: UsageRecord
    definition: >
      A record of resource consumption for a single request: provider, model,
      prompt/completion tokens, cost, latency, HTTP status, request_id. Stored in
      the usage_events table in Postgres.
    context: unknown
    aliases: [usage event, usage_event, metering record]
    source: [US-015]

  - id: TERM-007
    term: UsageBuffer
    definition: >
      A bounded in-memory buffer (a buffered Go channel) into which usage records
      are placed synchronously on the hot path. A background worker reads from the
      buffer and flushes batches to Postgres. When the buffer is full, the record
      is dropped and a counter is incremented.
    context: unknown
    aliases: [metering buffer]
    source: [US-015]

  - id: TERM-008
    term: BoundedConcurrency
    definition: >
      A limit on the maximum number of simultaneous requests to upstream providers,
      enforced via a semaphore or `errgroup.SetLimit`. When the pool is exhausted,
      new requests receive backpressure (503) instead of allowing unbounded goroutine
      growth.
    context: unknown
    aliases: [worker pool, bounded worker pool, bounded concurrency]
    source: [US-016]

  - id: TERM-009
    term: GracefulShutdown
    definition: >
      The procedure for orderly service shutdown upon receiving SIGINT/SIGTERM:
      waiting for in-flight requests to complete, performing a final flush of the
      metering buffer, closing connection pools, and logging the number of drained
      requests and dropped events.
    context: unknown
    aliases: [graceful drain, drain]
    source: [US-013]

  - id: TERM-010
    term: Backpressure
    definition: >
      A mechanism for quickly rejecting new requests (503 with Retry-After) when
      the worker pool is saturated. Prevents queue growth and goroutine consumption
      under overload.
    context: unknown
    aliases: [backpressure]
    source: [US-005, US-016]

  - id: TERM-011
    term: SSE
    definition: >
      Server-Sent Events — an HTTP response streaming protocol. The gateway forwards
      SSE chunks from the provider to the client via `http.Flusher`, terminating the
      stream when the client cancels.
    context: unknown
    aliases: [streaming, stream, SSE streaming]
    source: [US-002]

  - id: TERM-012
    term: RequestId
    definition: >
      A unique identifier for an HTTP request, generated by the gateway and added
      to structured slog logs, OTel traces, and the usage record. Used to correlate
      events within a single request.
    context: unknown
    aliases: [request_id, trace id, correlation id]
    source: [US-012, US-017]

  - id: TERM-013
    term: TokenBucket
    definition: >
      A rate-limiting algorithm in which throughput is determined by the rate at
      which virtual tokens are replenished. Implemented locally via
      `golang.org/x/time/rate` and in a distributed manner via Redis.
    context: unknown
    aliases: [token bucket]
    source: [US-005]

  - id: TERM-014
    term: Liveness
    definition: >
      A process-viability check via GET /healthz. Always returns 200 while the
      process is alive. Used by the orchestrator to restart a dead process.
    context: unknown
    aliases: [liveness probe, healthz, health check]
    source: [US-009]

  - id: TERM-015
    term: Readiness
    definition: >
      A service-readiness check via GET /readyz. Returns 200 only when both Redis
      and Postgres are available; otherwise returns 503. Used by the load balancer.
    context: unknown
    aliases: [readiness probe, readyz]
    source: [US-010]
