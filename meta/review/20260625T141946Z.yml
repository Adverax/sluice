version: 1
generated_at: "2026-06-25T14:19:46Z"
target: "card/014-streaming-resilience — cycle-2 working-tree delta (git diff)"
overall_score: 8.5
risk_level: low
review_lane: fast
reviewer: forge:review

coverage:
  read:
    - "internal/pool/pool.go"
    - "internal/pool/pool_test.go"
    - "internal/metrics/infer.go"
    - "internal/tracing/infer.go"
    - "internal/server/stream_resilience_test.go"
    - "internal/server/server.go"
    - "internal/proxy/resilience/resilience.go"
  not_reached:
    - "cmd/gateway/main.go (verified by grep only)"
  confidence: high
metrics:
  diff: { files: 5, added: 232, removed: 0 }
  max_fan_in: { symbol: "Pool.GuardStream", callers: 1 }
  tests_cover_change: true
  hotspots: []
  cochange_missing: []

root_causes: []

findings:
  - id: F-001
    severity: low
    category: tech-debt
    type: gap
    file: internal/server/stream_resilience_test.go
    line: 360
    title: "New test file is not gofmt-clean"
    description: |
      The slowSource.stopped field has a misaligned trailing comment (two spaces
      before the comment). `gofmt -l` reports the file. `make ci` runs `go vet`
      (which passes) but not gofmt, so the local gate misses it; golangci-lint
      (CON-004) would flag it if installed.
    suggestion: |
      Run `make fmt` (or `gofmt -w internal/server/stream_resilience_test.go`).
    confidence: high
    source: static
    root_cause: null
    status: open
    fixed_in: null
    dismissed_reason: null
  - id: F-002
    severity: info
    category: tech-debt
    type: question
    file: internal/server/stream_resilience_test.go
    line: 407
    title: "Unused router scaffolding in composed-chain test"
    description: |
      router := proxy.NewRouter(); router.Register("composed", src); _ = router
      is set up but never consulted — the composed StreamFunc receives `src`
      directly as the provider arg. The _ = router and comment are honest, but
      the three lines are dead setup. Not a defect.
    suggestion: |
      Optionally remove the router lines for clarity; the test passes src directly.
    confidence: high
    source: static
    root_cause: null
    status: open
    fixed_in: null
    dismissed_reason: null
